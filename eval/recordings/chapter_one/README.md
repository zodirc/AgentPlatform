# chapter_one 记录

选书卡停在 3.3 形状。下一份证据是**同一开头的第一章**，不是再出一组卡。

协议（不闸、不扩 `excerpt_job`、不加店招课）：

1. 采用冻结开头 `committed-two-coins.json`（《两枚硬币》，来自 2026-09-14 live）。这段原样作章首，不要重开。
2. 用户句保持题材连续，例如：「采用此开篇「两枚硬币」。写第一章。」不要同时改正例或书名规则。
3. 产品栈 live 跑 `draft_section`。长篇约一千八到四千五，一场写满。点选后仍灌 `SERIAL_AFTER_LOCK_CRAFT`（2026-09-16 起是「原样作开头」，不再要求早交得到）。
4. 保存到 `eval/recordings/chapter_one/<date>-two-coins-ch1.json`：
   - 冻结 `title` + `opening`（须与 committed 逐字相同；若模型重写了开头，记 `opening_rewritten: true`，本轮作废另跑）
   - 章稿正文（workspace `drafts/manuscript.md` 或工具回写）
   - 原始工具参数 vs 落盘（若有剪尾/拒修，分开存）
   - `turn.thinking.delta` 拼接到同目录 `<date>-two-coins-ch1.thinking.txt`（不要覆盖仓库根 `thinking.txt` 当唯一副本）
   - 当时注入的 `SERIAL_AFTER_LOCK_CRAFT` 原文
5. 人读一章，只记四问（**不写进提示、不当写作表**）：
   - 开头那件事（疤烫、雨绕开、楼顶的人）有没有继续占住时间、选择、注意力，还是很快丢掉、改去介绍设定
   - 新信息改没改人物下一步行动，还是只供读者理解世界
   - 与其他人的接触改没改走向，还是只递送规则和奖励
   - 章末留下的问题是不是这一章发生过的事长出来的，还是临时加一个更大的秘密
6. 不要求每章都有明确目标、冲突升级或章末悬念。不按「超凡第几字出现」打分。
7. n=1，题材仍是都市修真。不外推校园/历史。不回写选书阈值。

`灶上的锅盖` 留作第二份；先跑硬币，避免一次换两个开头。
