id: writing.09_export_document
scenario_id: writing
phase: 2
description: export_document exports an explicit confirmed section set

fixtures:
  workspace:
    - path: outline.md
      content: |
        # Export Test
        - Section A
    - path: sections/a.md
      content: |
        Section A body for export.
    - path: sections/junk.md
      content: |
        Historical junk must not be exported.

input:
  message: "writing.09 export_document 导出文档"
  client_request_id: "00000000-0000-4000-8000-000000000109"

model_mode: stub

assertions:
  events:
    sequence_contains:
      - tool.started
      - tool.completed
      - turn.completed
    payload_validates: true
  turn:
    status: completed
  workspace:
    - path: exports/document.md
      matches: "Section A body"
      not_matches: "Historical junk"

tags: [writing, export, p2]
