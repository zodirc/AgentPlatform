id: agent.01_read_and_edit_file
scenario_id: agent
phase: 1
description: Read file then edit_file (in-place after approval)

fixtures:
  workspace:
    - path: README.md
      content: "旧正文 for agent.01"

input:
  message: "读取 README.md 并 edit_file agent.01"
  client_request_id: "00000000-0000-4000-8000-000000000101"

model_mode: stub

commands:
  - type: approve-tool-call
    tool_call_id: auto

assertions:
  events:
    sequence_contains:
      - tool.started
      - approval.requested
      - approval.resolved
      - turn.completed
  turn:
    status: completed
  latency:
    ttfb_ms_max: 300

tags: [agent, edit, p1]
