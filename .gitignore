# secrets / local env
.env
.env.*
!.env.example

# python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
*.egg

# node / web
services/web/node_modules/
services/web/.pnpm-store/
services/web/dist/
*.tsbuildinfo

# editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# runtime / local artifacts
debug.log
.coverage
*.log
ci-proof.log
reports/eval-last-failures.txt

# agent runtime sandbox (bind-mounted; user-local)
workspace/
.eval-workspace/
eval/reports/
eval/plan_suggest/reports/

# Official benches: pulled corpora / SWE images live under BENCH_DATA_DIR (default
# ~/.cache/agentplatform-bench). Never commit datasets.
.cache/agentplatform-bench/
eval/official/.local-data/

# F4: local backups (pg_dump + volume tarballs)
deploy/backups/

# local compose overlay (from dev.override.yml.example)
deploy/compose/dev.override.yml

# nested empty clone placeholder (if recreated)
AgentPlatform/
interview-self-qa.md

# Intel vendor corpus (fetched Turn-外; see seed/sources/intel/README.md)
.cache/intel-corpus/
seed/sources/intel/vendor/**
!seed/sources/intel/vendor/.gitkeep
