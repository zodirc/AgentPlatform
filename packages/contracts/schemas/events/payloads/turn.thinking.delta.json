{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-platform.local/schemas/events/payloads/turn.thinking.delta.json",
  "title": "TurnThinkingDeltaPayload",
  "description": "Ephemeral reasoning/thinking text delta for live UI. Not projected into turn_views or session history.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "delta"
  ],
  "properties": {
    "delta": {
      "type": "string",
      "maxLength": 8192
    },
    "step_index": {
      "type": "integer",
      "minimum": 0
    },
    "subagent_id": {
      "type": "string",
      "minLength": 1,
      "description": "Present when the event originates from a nested delegate sub-agent."
    }
  }
}
