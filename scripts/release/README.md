# 部署看板（:9090）

**左**：每个检查项一行，要处理的旁边有按钮（重建 api / sync 索引…）  
**右**：点选某项看详情 + 任务日志  

自动刷新。本机访问操作不需要填 Secret。

```bash
make up                 # 分模块起栈并拉起看板
make release-plan       # 终端看同一份健康 JSON
http://127.0.0.1:9090/
```
