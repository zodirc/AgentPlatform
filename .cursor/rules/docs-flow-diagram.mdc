---
description: 文档流程图用「主链详流」。先对照 StartTurn 样板再画；禁止脚本/卡片堆；整张新手绘 PNG
globs: docs/**
alwaysApply: true
---

# 文档图：主链详流

仓库 `docs/assets/**/*.png` 是 **控制流详图**，不是说明卡片、信息图或 BPMN。

**动图前必须先打开样板对照像素，不得凭记忆发明皮肤：**  
`docs/assets/events/start-turn-command-zh.png`

其它已定稿图（`request-path-zh.png`、`agent-engine-loop-zh.png` 等）同属此范式；只改内容时也要长得像它们，不能长得像一套新皮肤。

## 何时适用

重绘、新增、替换任何 `docs/assets/**/*.png`，或改图注/正文里「图逐步经过」。无关编码任务则忽略本文件后半，但仍不得用脚本给文档出图。

## 出图原则（硬）

1. **整张重绘**成新的 1536×1024 RGB PNG。禁止在旧 PNG 上涂改、叠加文字、局部擦写、把旧图当底图修。
2. **禁止脚本/工厂出图**：Pillow 模板、HTML/CSS 排版再截图、Graphviz/mermaid 导出当正文图、等重 N 宫格循环盖章、Unicode `↓` 当箭头脊柱。这是原则，不是口味。
3. 冲突：**代码与契约 > 六篇正文 > 图上的字**。图负责看清分支；过期数字按代码改，皮肤仍跟样板。

## 页结构（对照样板）

一张图通常是：

| 区 | 职责 | 长什么样 |
|----|------|----------|
| 顶 | 居中标题 + 一行灰字副标题（旋钮/原则） | 不是左对齐海报标题 |
| 左辅栏 | 入口、触发、旁路 | 浅灰虚线分组；**白底细线小格**竖排 |
| **中主链** | 逐步经过 | **一条垂直脊柱**：超宽矮条带 + 左缘编号圆 + 画出来的黑箭头 |
| 右辅栏 | 侧注、禁止、只读/异步 | 彩色虚线分组；内部仍是白格或短句，不是第二套主链 |
| 底 | 「要点：」红虚线横条（红点 + `｜` 分隔）+ 左下图例 + 居中灰字路径 | 样板底栏那种，不是又堆三张卡片 |

主链条带：几乎留白的淡底、细色框、约一行技术中文（可混 `code`）；编号实心圆贴在**条带左缘外侧**，与框同色。分组虚线标题骑在框边上（如「主链」「要点」）。

判定：**黄菱形**，出边写「是」「否」（可 `是 · pull`）。支路用直角折线接回主链或落到否芯片。

并行只画原过程已经并行的段（如 Chunk / Doc）；不要把主链拆成三列等重卡片。

## 视觉权重（卡片式的判据）

读者应先看见 **中间那条从上到下的箭头链**，再看见左右注。

- 主链条带宽、矮、淡；左右芯片小、白、细框。
- 若满页都是差不多大的彩色圆角块，箭头只是块间装饰 → **卡片式，重画**。
- 若步骤写成一段营销 bullet、框里大片留白、编号徽章像 App 按钮 → **卡片式，重画**。
- 箭头必须是画出来的线 + 箭头，贯穿条带缝；禁止只靠 emoji/Unicode 充当流程。

## 构图变体（仍是同一范式）

- **单主链**（最像样板）：`start-turn-command`、`index-sync`、`agent-engine-loop`。
- **主链 + 嵌套白格**：组窗卫生 2.1/2.2/2.3 是主链第 2 步里的白格，不是四张独立卡片。
- **并行子系统 + 下方主链判定**：`search-sources` 上框里三条车道用白格，ACL/cover 仍回到宽条带 + 菱形。
- **多列阶段 + 底环**：`coding-fuse` 上三列是虚线分组包白格；底下一行仍是 Engine while 环，虚线汇入 `tool_use`。

## 完成前自检

- 和样板并排：分组虚线、条带宽高、编号圆、菱形、要点横条是否同类？
- 不看颜色，能否只靠箭头从步骤 1 走到终态/回灌？
- 路径页脚是否为该 PNG 的 `docs/assets/...`？
- 图内数字是否仍为现行实现（切块 450/64、组窗 4k·32k、fill 0.80/0.90/0.95 等）？
