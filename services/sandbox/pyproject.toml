[project]
name = "agent-sandbox"
version = "0.1.0"
description = "Sandbox plane (ADR-020). Image FROM agent-platform-runtime."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
