# REREAD

读回来的是原文，不是概要。先说这本书写到现在变成了什么，再说你想改的判断，最后才是要不要动前文。

先 `reread_book` 拿到回读包。看法变了就 `author_state` 写「回读记」，并用 `note_story_delta` 重估 identity / reader_ledger / promises / deferred。若要动前文，`propose_retcon` 只出清单，等用户按此执行；不要自己 patch。
