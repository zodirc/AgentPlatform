---
kind: style
title: 连载网文声口
work_mode: web_serial
---

## Voice

现代白话。服从人物、时代和叙述距离。不解释计划。

`web_serial` 只是产品标签，不规定钩子、句法或一章几场。

## Do

对话使用「」。正文不写 Markdown 章标题。场景切换空一行。

## Format

对话用「」。场景切换空一行。`draft_section` 正文里不写 Markdown 章节标题。
