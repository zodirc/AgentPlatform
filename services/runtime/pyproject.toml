[project]
name = "agent-runtime"
version = "0.1.0"
description = "Agent Platform Runtime"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.27",
    "asyncpg>=0.29",
    "psycopg[binary]>=3.1",
    "httpx>=0.27",
    "cryptography>=42.0",
    "pydantic>=2.6",
    "pydantic-settings>=2.2",
    "agent-contracts>=0.1.0",
    "pyyaml>=6.0",
    "jsonschema>=4.21",
    "langgraph>=0.2",
    "ruff>=0.3",
    "structlog>=24.0",
    "opentelemetry-api>=1.27.0",
    "opentelemetry-sdk>=1.27.0",
    "opentelemetry-instrumentation-fastapi>=0.48b0",
    "opentelemetry-exporter-otlp-proto-http>=1.27.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "pytest-cov>=5.0",
    "ruff>=0.3",
]
retrieval = [
    "sentence-transformers>=3.0",
    "numpy>=1.26",
]
structural = [
    "jedi-language-server>=0.41",
    "tree-sitter>=0.23",
    "tree-sitter-language-pack>=0.7",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.coverage.run]
source = ["app"]
omit = [
    "app/main.py",
    # LSP client/pool are integration-heavy; covered by dedicated structural tests
    # without counting cold paths toward the global 80% gate.
    "app/structural/client.py",
    "app/structural/pool.py",
    "app/structural/adapters.py",
]

[tool.coverage.report]
fail_under = 80
include = [
    "app/tools/core/*",
    "app/engine/*",
    "app/graph/*",
    "app/scenarios/registry.py",
    "app/observability/*",
    "app/context/engine.py",
    "app/controller/input_compiler.py",
    "app/tools/bootstrap.py",
    "app/tools/registry.py",
    "app/retrieval/*",
    "app/structural/format.py",
    "app/structural/providers.py",
    "app/structural/types.py",
    "app/structural/__init__.py",
]
